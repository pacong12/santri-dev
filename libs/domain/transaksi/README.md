# transaksi

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build transaksi` to build the library.

## Running unit tests

Run `nx test transaksi` to execute the unit tests via [Vitest](https://vitest.dev/).
