# tagihan

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tagihan` to build the library.

## Running unit tests

Run `nx test tagihan` to execute the unit tests via [Vitest](https://vitest.dev/).
