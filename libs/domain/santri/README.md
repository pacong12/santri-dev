# santri

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build santri` to build the library.

## Running unit tests

Run `nx test santri` to execute the unit tests via [Vitest](https://vitest.dev/).
