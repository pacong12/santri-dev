# invoice

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build invoice` to build the library.

## Running unit tests

Run `nx test invoice` to execute the unit tests via [Vitest](https://vitest.dev/).
